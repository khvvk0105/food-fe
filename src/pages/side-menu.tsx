import SideMenu from "@/components/SideMenu";
import { Box } from "@mui/material";

const sidemenu = () => {
  return (
    <Box width={"200px"} display={"flex"} justifyContent={"start"}>
      <SideMenu />
    </Box>
  );
};
export default sidemenu;
